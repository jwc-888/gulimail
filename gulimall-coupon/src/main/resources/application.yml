spring:
    datasource:
        username: root
        password: jwc
        url: jdbc:mysql://100.85.14.120:3307/gulimall_sms?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
        driver-class-name: com.mysql.cj.jdbc.Driver
    cloud:
        nacos:
            discovery:
                server-addr: 127.0.0.1:8848
            config:
                server-addr: 127.0.0.1:8848
                file-extension: properties # 和 Nacos 配置后缀一致
        loadbalancer:
            ribbon:
                enabled: false  # 彻底关闭 Ribbon
    application:
        name: gulimall-coupon

mybatis-plus:
    mapper-locations: classpath:/mapper/**/*.xml
    global-config:
        db-config:
            id-type: auto

feign:
    sentinel:
        enabled: true

#暴露所有端点
management:
    endpoints:
        web:
            exposure:
                include: '*'

server:
    port: 8002