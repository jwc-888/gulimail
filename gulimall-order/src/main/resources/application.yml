spring:
  datasource:
    druid:
      username: root
      password: jwc
      url: jdbc:mysql://100.85.14.120:3307/gulimall_oms?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
      driver-class-name: com.mysql.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 10.194.189.67:8848
    loadbalancer:
      ribbon:
        enabled: false  # 彻底关闭 Ribbon
  application:
    name: gulimall-order

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
    port:8003
